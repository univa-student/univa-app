/**
 * pages/spaces/spaces.page.tsx
 *
 * Spaces (Спейси) page — stub.
 */

export function SpacesPage() {
    return (
        <div className="flex flex-col gap-4">
            <h1 className="text-2xl font-semibold">Спейси</h1>
            <p className="text-muted-foreground">
                Сторінка спейсів — в розробці
            </p>
        </div>
    );
}
