/**
 * widgets/notifications-center/index.tsx
 *
 * NotificationsCenter — dropdown/panel listing recent notifications.
 * Stub: replace with real notification list.
 */

export function NotificationsCenter() {
    return (
        <section className="notifications-center" aria-label="Notifications center">
            {/* TODO: fetch and list Notification entities + mark-read */}
            <p className="text-muted-foreground text-sm">
                Центр сповіщень — скоро буде реалізовано
            </p>
        </section>
    );
}
